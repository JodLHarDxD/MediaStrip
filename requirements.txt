fastapi>=0.111.0
opencv-python-headless>=4.9.0
uvicorn[standard]>=0.29.0
yt-dlp[default]>=2026.6.9
Pillow>=10.3.0
python-multipart>=0.0.9
aiofiles>=23.2.1
requests>=2.32.0
slowapi>=0.1.9
httpx>=0.27.0
beautifulsoup4>=4.12.0
curl_cffi>=0.7.0
# yt-dlp plugin: lets the YouTube extractor pull GVS PO tokens from a bgutil HTTP
# provider (set MS_YT_POT_BASE_URL). Required for cookieless server-side YouTube
# downloads from datacenter IPs (otherwise: "unable to download video data: 403").
bgutil-ytdlp-pot-provider>=1.1.0
# torch + torchvision: install separately with CUDA wheel for GPU watermark removal
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# pip install simple-lama-inpainting
